services:
  - type: web
    name: ai-code-review
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./build
    envVars:
      - key: CI
        value: false
      - key: NODE_VERSION
        value: 20